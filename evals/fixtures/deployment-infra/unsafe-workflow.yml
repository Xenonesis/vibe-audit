# SYNTHETIC TEST FIXTURE — Used exclusively by static analyzer evals
name: unsafe-fixture
on: [pull_request]
permissions: write-all
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "MOCK_SECRET_ECHO_SYNTHETIC"
