# API / Backend Profile

Use for APIs, services, workers, or server-only applications.

Prioritize authentication/authorization, object ownership, validation, injection, SSRF, rate/abuse controls, transactional writes, idempotency, timeouts/retries, pagination/response bounds, query efficiency, logging/error disclosure, and backward-compatible public contracts.
