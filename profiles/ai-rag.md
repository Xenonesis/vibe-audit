# AI / RAG / Agent Profile

Load `references/ai-app-security.md` plus relevant core references.

Treat model output, retrieved content, tool responses, documents, and memory as untrusted. Review prompt/tool injection boundaries, data exfiltration, authorization before tool execution, tenant/vector isolation, sensitive prompt/logging, unsafe HTML/Markdown rendering, output validation, cost/resource abuse, and fallback/fake-success behavior.
