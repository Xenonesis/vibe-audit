# Default Profile

Use when no narrower specialist profile clearly fits.

Load the active mode's required core references only. Preserve architecture, establish a static-first baseline, and expand scope only when the user asks or a severe out-of-scope risk must be reported.
