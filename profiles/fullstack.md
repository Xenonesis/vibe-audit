# Full-Stack Profile

Use when frontend, backend/API, persistence, and auth/business flows interact.

Trace requests across browser → API → authorization → validation → database → external services. Prioritize ownership/tenant checks, server authority, transactional correctness, idempotency, error semantics, N+1/query behavior, and end-to-end verification.
