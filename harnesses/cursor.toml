id = "cursor"
binary = "agent"
fallback_binaries = ["cursor"]
version_args = ["--version"]
headless = true
native_skills = true
explicit_skill_invocation = true
docs_checked = "2026-08-11"
notes = "Headless agent may have write/shell capability; outer sandbox is mandatory for autonomous adversarial evals."
