id = "omp"
binary = "omp"
version_args = ["--version"]
native_skills = true
explicit_skill_invocation = true
headless = false
docs_checked = "2026-08-11"
notes = "Do not assume automatic routing; use explicit skill invocation when deterministic behavior is required."
