id = "opencode"
binary = "opencode"
version_args = ["--version"]
headless = true
native_skills = true
explicit_skill_invocation = true
docs_checked = "2026-08-11"
notes = "OpenCode natively supports Agent Skills loaded on demand through its `skill` tool."